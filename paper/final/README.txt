% IEEE TCSS TriFuse — figure index
%
% All figures are vector LaTeX (TikZ / pgfplots), sized for single-column IEEEtran.
% Merged panels follow reviewer guidance: Fig.~3 (branches + fusion), Fig.~7 (ablation + training).
%
% fig01_preprocessing.tex   — Text preprocessing pipeline
% fig02_architecture.tex    — TriFuse end-to-end architecture
% fig03_branches_fusion.tex — (a) Multi-view branches + (b) Attention fusion [MERGED]
% fig04_training_pipeline.tex — Experimental training protocol
% fig05_baseline_comparison.tex — Baseline bar chart (Table IV)
% fig06_single_branch.tex   — Single-branch vs full TriFuse (Table VI)
% fig07_ablation_training.tex — (a) Ablation bars + (b) Training curves [MERGED]
% fig08_attention_fusion.tex — Attention weighting detail (ablation section)
%
% Usage in IEEE_TCSS_TriFuse.tex:
%   \input{final/fig0N_...}
%
% Dependencies (in main preamble):
%   \usepackage{tikz, pgfplots}
%   \usetikzlibrary{..., backgrounds}
%   \input{final/common_styles.tex}
