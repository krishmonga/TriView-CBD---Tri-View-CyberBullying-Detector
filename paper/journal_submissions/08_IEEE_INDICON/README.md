# IEEE INDICON (Conference)

| Item | Detail |
|------|--------|
| **Publication fee** | **No APC** — pay conference registration only |
| **Portal** | Check current IEEE INDICON CFP (India Council) |
| **Format** | IEEE two-column conference (`IEEEtran` conference mode) |
| **Difficulty** | Medium — faster than journals |

## Important

Conference papers have a **page limit** (often 4–6 pages). The current draft is full journal length. **Shorten** before final INDICON submission (merge sections, move ablation to appendix if allowed).

## Compile

```bash
cd paper/journal_submissions/08_IEEE_INDICON
pdflatex TriFuse.tex && pdflatex TriFuse.tex
```

## Files

- `TriFuse.tex` — IEEE conference wrapper
- `COVER_LETTER.txt`
